name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [22]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run Linting
        run: npm run lint
        
      - name: Run Unit tests with coverage
        id: unit-tests
        run: npm run test:coverage
        continue-on-error: true
        
      - name: Check unit test results file
        if: always() && matrix.node-version == 22
        run: |
          echo "=== After unit tests ==="
          ls -lah test-results/ 2>/dev/null || echo "test-results directory does not exist"
          if [ -f test-results/unit/junit.xml ]; then
            echo "✓ junit.xml created"
            head -20 test-results/unit/junit.xml
          else
            echo "✗ junit.xml NOT created"
            echo "Checking if vitest wrote the file elsewhere:"
            find . -name "junit.xml" -type f 2>/dev/null || echo "No junit.xml found anywhere"
          fi
        
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
        
      - name: Run E2E tests
        id: e2e-tests
        run: npm run test:e2e
        continue-on-error: true
        
      - name: Verify test result files
        if: always() && matrix.node-version == 22
        run: |
          echo "=== Checking test-results directory structure ==="
          find test-results -type f -name "*.xml" 2>/dev/null || echo "No XML files found"
          echo ""
          echo "=== Unit test results ==="
          if [ -f test-results/unit/junit.xml ]; then
            echo "✓ junit.xml found ($(wc -l < test-results/unit/junit.xml) lines)"
            echo "  Test cases: $(grep -c '<testcase' test-results/unit/junit.xml || echo '0')"
          else
            echo "✗ junit.xml NOT FOUND"
          fi
          echo ""
          echo "=== E2E test results ==="
          if [ -f test-results/playwright/junit.xml ]; then
            echo "✓ playwright/junit.xml found ($(wc -l < test-results/playwright/junit.xml) lines)"
            echo "  Test cases: $(grep -c '<testcase' test-results/playwright/junit.xml || echo '0')"
          else
            echo "✗ playwright/junit.xml NOT FOUND"
          fi
        
      - name: Upload Playwright report
        if: always() && matrix.node-version == 22
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
        
      - name: Publish Test Results
        if: always() && matrix.node-version == 22
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: |
            test-results/unit/junit.xml
            test-results/playwright/junit.xml
          check_name: Test Results
        
      - name: Archive test results
        if: always() && matrix.node-version == 22
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          retention-days: 30

      - name: Upload coverage reports
        if: always() && matrix.node-version == 22
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: coverage/
          retention-days: 30

      - name: Report Coverage
        if: always() && matrix.node-version == 22
        uses: davelosert/vitest-coverage-report-action@v2
        with:
          json-summary-path: ./coverage/coverage-summary.json
          json-final-path: ./coverage/coverage-final.json

      - name: Check for test failures
        if: always() && matrix.node-version == 22
        run: |
          if [ "${{ steps.unit-tests.outcome }}" == "failure" ] || [ "${{ steps.e2e-tests.outcome }}" == "failure" ]; then
            echo "One or more test suites failed"
            exit 1
          fi

